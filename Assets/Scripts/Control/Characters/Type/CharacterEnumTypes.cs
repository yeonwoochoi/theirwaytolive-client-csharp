namespace Control.Characters.Type
{
    public enum Direction
    {
        Left, Right, Up, Down
    }
    
    public enum HeroControlType
    {
        Joystick, Auto, Disable
    }

    public enum MainCharacterType
    {
        Bachi, Duju, Panno, Etc
    }

    public enum EnemyActionType
    {
        Attack, Escape, Detect,
    }

    public enum DetectModeType
    {
        Circle, Sector
    }
}